node_modules
dist
.DS_Store
.vite
*.log

# Tauri
src-tauri/target
src-tauri/gen/schemas

# Updater private key — never commit. Public key is in tauri.conf.json.
.updater-key
*.updater-key

# Local-only scraped data
.nvidia-series.xml

# Test harness output — sweeps, install-media artifacts, scratch reports.
# Reproducible via scripts/test-tweaks-in-vm.ps1, scripts/test-install-media.ps1,
# scripts/gen-unattend-config.mjs. Never committed.
test-output/

# Claude Code internal state
.claude/

# pnpm regenerates this when it sees onlyBuiltDependencies in package.json,
# but the auto-generated file lacks `packages:` which breaks pnpm 9 in CI.
# The canonical config lives in package.json under "pnpm".
pnpm-workspace.yaml
